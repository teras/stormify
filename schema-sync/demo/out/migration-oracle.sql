-- schema-sync migration
-- Generated 2026-04-28 for oracle

-- ── CREATE addresses (entity com.example.shop.Address) ──
CREATE TABLE addresses (
    id NUMBER(19) GENERATED BY DEFAULT AS IDENTITY NOT NULL PRIMARY KEY,
    owner NUMBER(19) DEFAULT 0 NOT NULL,
    street VARCHAR(200) DEFAULT '' NOT NULL,
    city VARCHAR(50) DEFAULT '' NOT NULL,
    state VARCHAR(50) DEFAULT '' NOT NULL,
    zip VARCHAR(10) DEFAULT '' NOT NULL,
    country VARCHAR(50) DEFAULT '' NOT NULL,
    is_primary NUMBER(1) DEFAULT 0 NOT NULL,
    validated_at TIMESTAMP WITH TIME ZONE
);

-- ── agg_test (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   name  VARCHAR2(4000)

-- ── all_types (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   byte_val  NUMBER(5)
--   short_val  NUMBER(5)
--   int_val  NUMBER(10)
--   long_val  NUMBER(19)
--   float_val  BINARY_FLOAT
--   double_val  BINARY_DOUBLE
--   bool_val  NUMBER(1)
--   string_val  VARCHAR2(4000)

-- ── annotated_test (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   custom_col  VARCHAR2(4000)
--   read_only_on_create  VARCHAR2(4000)
--   read_only_on_update  VARCHAR2(4000)

-- ── at_child (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   name  VARCHAR2(4000)
--   is_active  NUMBER(1)
--   parent  NUMBER(10)

-- ── audit_events (DB only) ──
-- table exists in DB but no matching entity
--   event_id  NUMBER(19)
--   payload  CLOB

-- ── CREATE audit_log (entity com.example.shop.AuditLog) ──
CREATE TABLE audit_log (
    id NUMBER(19) GENERATED BY DEFAULT AS IDENTITY NOT NULL PRIMARY KEY,
    actor NUMBER(19) DEFAULT 0 NOT NULL,
    action VARCHAR(50) DEFAULT '' NOT NULL,
    entity_type VARCHAR(50) DEFAULT '' NOT NULL,
    entity_id VARCHAR(50) DEFAULT '' NOT NULL,
    occurred_at TIMESTAMP WITH TIME ZONE
);

-- ── auto_child (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   data  VARCHAR2(4000)
--   parent  NUMBER(10)

-- ── auto_increment (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   name  VARCHAR2(4000)

-- ── auto_parent (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   other  VARCHAR2(4000)
--   data  VARCHAR2(4000)

-- ── bd_test (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   val  NUMBER(38,10)

-- ── bi_test (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   val  NUMBER(38)

-- ── blacklist_test (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   name  VARCHAR2(4000)
--   secret  VARCHAR2(4000)

-- ── blob_test (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   blob_data  BLOB
--   clob_as_chars  VARCHAR2(4000)
--   clob_as_string  VARCHAR2(4000)

-- ── camel_entity (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   first_name  VARCHAR2(4000)
--   last_name  VARCHAR2(4000)

-- ── camelpolicyentity (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   firstname  VARCHAR2(4000)
--   lastname  VARCHAR2(4000)

-- ── CREATE categories (entity com.example.shop.Category) ──
CREATE TABLE categories (
    id RAW(16) NOT NULL PRIMARY KEY,
    name VARCHAR(50) DEFAULT '' NOT NULL,
    description VARCHAR(1000),
    parent RAW(16),
    sort_order NUMERIC(10) DEFAULT 0 NOT NULL
);

-- ── child (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   name  VARCHAR2(4000)
--   parent  NUMBER(10)

-- ── conversion_test (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   value  VARCHAR2(4000)

-- ── ct_test (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   name  VARCHAR2(4000)

-- ── double_db_name (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   name  VARCHAR2(4000)

-- ── dual_key (DB only) ──
-- table exists in DB but no matching entity
--   id1  NUMBER(10)
--   id2  NUMBER(10)
--   data  VARCHAR2(4000)

-- ── enc_cols (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   col_iso1  VARCHAR2(4000)
--   col_iso7  VARCHAR2(4000)
--   col_u8  VARCHAR2(4000)
--   col_u16  VARCHAR2(4000)

-- ── enum_notnull_test (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   status  NUMBER(10)

-- ── enum_string_test (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   status  VARCHAR2(4000)
--   priority  NUMBER(10)

-- ── enum_test (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   plain_status  NUMBER(10)
--   custom_status  NUMBER(10)

-- ── event (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   title  VARCHAR2(4000)
--   event_date  DATE

-- ── findall_test (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   name  VARCHAR2(4000)

-- ── fly_test (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   name  VARCHAR2(4000)

-- ── generic_test (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   value  VARCHAR2(4000)

-- ── CREATE invoices (entity com.example.shop.Invoice) ──
CREATE TABLE invoices (
    id NUMBER(19) DEFAULT 0 NOT NULL PRIMARY KEY,
    order_id NUMBER(19) DEFAULT 0 NOT NULL,
    amount NUMERIC(14,2) DEFAULT 0 NOT NULL,
    paid NUMBER(1) DEFAULT 0 NOT NULL,
    issued_at TIMESTAMP WITH TIME ZONE,
    due_date DATE,
    generated_at TIMESTAMP WITH TIME ZONE
);

-- ── large_test (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   value  CLOB

-- ── legacy_carts (DB only) ──
-- table exists in DB but no matching entity
--   cart_id  NUMBER(10)
--   user_id  NUMBER(10)
--   created_at  TIMESTAMP(6)

-- ── map_test (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   name  VARCHAR2(4000)

-- ── nested_level1 (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   label  VARCHAR2(4000)

-- ── nested_level2 (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   info  VARCHAR2(4000)

-- ── nested_level3 (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   tag  VARCHAR2(4000)

-- ── nt_test (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   name  VARCHAR2(4000)

-- ── nullable_test (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   name  VARCHAR2(4000)
--   nullable_int  NUMBER
--   nullable_string  VARCHAR2(4000)

-- ── CREATE order_items (entity com.example.shop.OrderItem) ──
CREATE TABLE order_items (
    id NUMBER(19) GENERATED BY DEFAULT AS IDENTITY NOT NULL PRIMARY KEY,
    order_id NUMBER(19) DEFAULT 0 NOT NULL,
    quantity NUMERIC(10) DEFAULT 1 NOT NULL,
    unit_price NUMERIC(14,2) DEFAULT 0 NOT NULL,
    discount_percent NUMERIC(3,2) DEFAULT 0 NOT NULL
);
-- WARNING: 1 fields skipped due to missing slot assignment

-- ── orders ──
ALTER TABLE orders ADD currency VARCHAR(10) DEFAULT 'EUR' NOT NULL;
ALTER TABLE orders ADD notes VARCHAR(1000);
ALTER TABLE orders ADD status_ordinal NUMERIC(1) DEFAULT 0 NOT NULL;
ALTER TABLE orders ADD placed_at TIMESTAMP WITH TIME ZONE;
ALTER TABLE orders ADD shipped_at TIMESTAMP WITH TIME ZONE;

-- ── CREATE payments (entity com.example.shop.Payment) ──
CREATE TABLE payments (
    id NUMBER(19) GENERATED BY DEFAULT AS IDENTITY NOT NULL PRIMARY KEY,
    invoice NUMBER(19) DEFAULT 0 NOT NULL,
    method VARCHAR(50) DEFAULT '' NOT NULL,
    transaction_ref VARCHAR(200) DEFAULT '' NOT NULL,
    amount NUMERIC(14,2) DEFAULT 0 NOT NULL,
    occurred_at TIMESTAMP WITH TIME ZONE
);

-- ── pk_resolver_test (DB only) ──
-- table exists in DB but no matching entity
--   pk_id  NUMBER
--   name  VARCHAR2(4000)

-- ── plain_child (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   name  VARCHAR2(4000)
--   is_active  NUMBER(1)
--   parent  NUMBER(10)

-- ── CREATE product_tags (entity com.example.shop.ProductTag) ──
CREATE TABLE product_tags (
    id NUMBER(19) GENERATED BY DEFAULT AS IDENTITY NOT NULL PRIMARY KEY,
    tag NUMBER(10) DEFAULT 0 NOT NULL
);
-- WARNING: 1 fields skipped due to missing slot assignment

-- ── products ──
ALTER TABLE products ADD category RAW(16) NOT NULL;
ALTER TABLE products ADD in_stock NUMBER(1) DEFAULT 1 NOT NULL;
ALTER TABLE products ADD launch_date DATE;
ALTER TABLE products ADD last_updated_at TIMESTAMP WITH TIME ZONE;

-- ── ref_child (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   title  VARCHAR2(4000)
--   parent  NUMBER(10)

-- ── ref_parent (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   name  VARCHAR2(4000)

-- ── refl_enum_string_test (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   status  VARCHAR2(4000)
--   priority  NUMBER(10)

-- ── refl_enum_test (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   plain_status  NUMBER(10)
--   custom_status  NUMBER(10)

-- ── CREATE reviews (entity com.example.shop.Review) ──
CREATE TABLE reviews (
    id NUMBER(19) GENERATED BY DEFAULT AS IDENTITY NOT NULL PRIMARY KEY,
    author NUMBER(19) DEFAULT 0 NOT NULL,
    rating NUMERIC(1) DEFAULT 5 NOT NULL,
    title VARCHAR(50) DEFAULT '' NOT NULL,
    body VARCHAR(1000),
    verified NUMBER(1) DEFAULT 0 NOT NULL,
    posted_at TIMESTAMP WITH TIME ZONE
);
-- WARNING: 1 fields skipped due to missing slot assignment

-- ── CREATE settings (entity com.example.shop.Setting) ──
CREATE TABLE settings (
    "key" VARCHAR(50) DEFAULT '' NOT NULL PRIMARY KEY,
    "value" VARCHAR(1000) DEFAULT '' NOT NULL,
    description VARCHAR(1000)
);

-- ── stress_table (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   data  VARCHAR2(4000)

-- ── strict_test (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   name  VARCHAR2(4000)

-- ── CREATE tags (entity com.example.shop.Tag) ──
CREATE TABLE tags (
    id NUMBER(10) GENERATED BY DEFAULT AS IDENTITY NOT NULL PRIMARY KEY,
    label VARCHAR(50) DEFAULT '' NOT NULL,
    color VARCHAR(10) DEFAULT '#000000' NOT NULL
);

-- ── test (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   name  VARCHAR2(4000)

-- ── tree_node (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   name  VARCHAR2(4000)
--   parent  NUMBER(10)

-- ── tx_ret (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   name  VARCHAR2(4000)

-- ── tx_test (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   name  VARCHAR2(4000)

-- ── unicode_test (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   value  VARCHAR2(4000)

-- ── upper_policy_entity (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   first_name  VARCHAR2(4000)
--   last_name  VARCHAR2(4000)

-- ── user_entity (DB only) ──
-- table exists in DB but no matching entity
--   id  NUMBER(10)
--   name  VARCHAR2(4000)
--   email  VARCHAR2(4000)
--   created_by  VARCHAR2(4000)

-- ── users ──
ALTER TABLE users ADD bio VARCHAR(1000);
ALTER TABLE users ADD email_verified NUMBER(1) DEFAULT 0 NOT NULL;
ALTER TABLE users ADD created_at TIMESTAMP WITH TIME ZONE;
ALTER TABLE users ADD last_login_at TIMESTAMP WITH TIME ZONE;

-- ── CREATE wishlists (entity com.example.shop.Wishlist) ──
CREATE TABLE wishlists (
    id RAW(16) NOT NULL PRIMARY KEY,
    owner NUMBER(19) DEFAULT 0 NOT NULL,
    name VARCHAR(50) DEFAULT '' NOT NULL,
    public_shared NUMBER(1) DEFAULT 0 NOT NULL,
    created_at TIMESTAMP WITH TIME ZONE
);

